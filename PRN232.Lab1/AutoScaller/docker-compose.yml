# version: "3.9"

services:
  web:
    image: prn232.lab1
    # networks:
    #   - app_network
    ports:
      - "7070:8080"
      - "7071:8081"
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
      resources:
        limits:
          cpus: "0.5"
          memory: 512M
# networks:
#   app_network:
#     driver: bridge
